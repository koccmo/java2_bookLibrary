package dental_clinic_tests.dependency_injection.junit;
/*
import dental_clinic.console_ui.patient.AddPatientUIAction;
import dental_clinic.dependency_injection.DIComponentFilter;
import org.junit.Test;

import java.util.ArrayList;
import java.util.List;

import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;


public class DIComponentFilterJUnitTest {

    private DIComponentFilter diComponentFilter = new DIComponentFilter();

    @Test
    public void test(){
        List<Class> classList = new ArrayList<>();
        classList.add(AddPatientUIAction.class);
        classList.add(DeletePatientUIAction.class);
        classList.add(DeletePatientRequest.class);
        classList.add(DeletePatientResponse.class);

        List<Class> expectedList = diComponentFilter.filter(classList);

        assertTrue(expectedList.size() == 0);
    }

}*/

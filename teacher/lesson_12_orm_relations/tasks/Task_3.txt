Так как класс ReaderBook является частью
объектно ориентированной модели с которой работает Java приложение,
поэтому напрашивается создание поля:

    private Reader reader;

и работа сразу с объектом читатель, а не его reader_id.

Для мапинга связей в ORM можно воспользоваться аннотацией
@ManyToOne следующим образом:

	@ManyToOne
	@JoinColumn(name = "reader_id", nullable = false)
	private Reader reader;

@ManyToOne - маппинг много к одному, один читатель может взять
в библиотеке много книг.

@JoinColumn - указывает колонку в базе данных, которая является
вторичным ключём, при помощи которой можно связать таблицы
reader_books и readers.